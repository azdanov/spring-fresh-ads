version: '3.9'
services:
  db:
    image: 'postgres:12'
    restart: always
    volumes:
      - "postgres_data:/var/lib/postgresql/data"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: fresh
      POSTGRES_USER: fresh
      POSTGRES_PASSWORD: fresh
volumes:
  postgres_data:
